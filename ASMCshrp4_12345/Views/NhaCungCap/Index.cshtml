@model IEnumerable<ASMCshrp4_12345.Models.Nhacungcap>
@{Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<h1>Nhà Cung Cấp</h1>
<form asp-action="Index" method="get" style="margin-top: 20px;">
    <div class="form-row d-flex align-items-center">
        <!-- Ô tìm kiếm -->
        <div class="col-md-5 mb-3">
            <input type="text" name="searchString" class="form-control" placeholder="Tìm kiếm theo tên hoặc mã nhà cung cấp" />
        </div>

        <!-- Tùy chọn sắp xếp -->
        <div class="col-md-3 mb-3">
            <div class="dropdown w-100">
                <button class="btn btn-outline-secondary dropdown-toggle w-100" type="button" id="sortMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fa fa-sort"></i> Sắp xếp theo
                </button>
                <div class="dropdown-menu" aria-labelledby="sortMenuButton">
                    <button type="submit" name="sortOrder" value="AZ" class="dropdown-item">
                        <i class="fa fa-arrow-up"></i> Tên nhà cung cấp (A-Z)
                    </button>
                    <button type="submit" name="sortOrder" value="ZA" class="dropdown-item">
                        <i class="fa fa-arrow-down"></i> Tên nhà cung cấp (Z-A)
                    </button>
                </div>
            </div>
        </div>

        <!-- Button Tìm kiếm -->
        <div class="col-md-2 mb-3">
            <button type="submit" class="btn btn-primary w-100">Tìm kiếm</button>
        </div>
    </div>
</form>
<p>
    <a asp-action="Create" class="btn btn-success Tao">Tạo Nhà Cung Cấp Mới</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Mã Nhà Cung Cấp</th>
            <th>Tên Nhà Cung Cấp</th>
            <th style="width:380px;">Địa Chỉ</th>
            <th>Email</th>
            <th>Số Điện Thoại</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MaNhaCc</td>
                <td>@item.TenNhaCc</td>
                <td>@item.DiaChi</td>
                <td>@item.Email</td>
                <td>@item.Sdt</td>
                <td>
                    <a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.MaNhaCc">Sửa</a>

                    <form asp-action="Delete" asp-asp-controller="NhaCungCap" asp-route-id="@item.MaNhaCc" method="post" style="display:inline;" onsubmit="return confirm('Bạn có chắc chắn muốn xóa nhà cung cấp này không?');">
                        <button type="submit" class="btn btn-danger">Xóa</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
