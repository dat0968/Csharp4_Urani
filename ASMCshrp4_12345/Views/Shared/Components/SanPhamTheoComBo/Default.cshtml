@model IEnumerable<ASMCshrp4_12345.Models.ComBo>
<div class="container-fluid pt-5 pb-3">
    <h2 class="section-title position-relative text-uppercase mx-xl-5 mb-4"><span class="bg-secondary pr-3">GIẢM GIÁ THEO COMBO</span></h2>
    <div class="row px-xl-5">
        @foreach (var item in Model.Take(4))
        {
            <div class="col-lg-3 col-md-4 col-sm-6 pb-1 cart">
                <div class="product-item bg-light mb-4">
                    <div class="product-img position-relative overflow-hidden">
                        <img style="width: 100px; height: 250px; border-radius: 10px; object-fit:cover" class="img-fluid w-100" src="~/HinhAnh/@item.AnhComBos?.FirstOrDefault()?.HinhAnh" alt="">
                        <div class="product-action">
                            <a asp-action="Details" asp-controller="CuaHang" asp-route-id="@item.MaComBo" class="btn btn-outline-dark btn-square">Xem Thêm</a>
                            @* <a class="btn btn-outline-dark btn-square" href=""><i class="fa fa-shopping-cart"></i></a>
                        <a class="btn btn-outline-dark btn-square" href=""><i class="far fa-heart"></i></a>
                        <a class="btn btn-outline-dark btn-square" href=""><i class="fa fa-sync-alt"></i></a>
                        <a class="btn btn-outline-dark btn-square" href=""><i class="fa fa-search"></i></a> *@
                        </div>
                    </div>
                    <div class="text-center py-4">
                        <a asp-action="DetailCombo" asp-controller="CuaHang" asp-route-idcombo="@item.MaComBo" class="h6 text-decoration-none text-truncate">@item.TenComBo</a>
                        <div class="d-flex align-items-center justify-content-center mt-2">
                            <h5>@item.DonGia.ToString("#,##0") đ</h5> <h6 class="text-muted ml-2">
                                <del>
                                    @(item.CtComBos?.Sum(item => item.MaSpNavigation.DonGiaBan * item.MaSpNavigation.SoLuongBan)) đ
                                </del>
                            </h6>
                        </div>
                        
                    </div>
                </div>
            </div>
        }
    </div>
</div>
<style scoped>

    .btn-square {
        border-radius: 10px;
        white-space: nowrap;
        padding: 0.5rem 1rem;
        font-size: 1rem;
        display: inline-flex;
        align-items: center;
        white-space: nowrap;
        padding: 0.5rem 1rem;
        min-width: 120px; /* Đặt chiều rộng tối thiểu */
        min-height: 40px;
        display: inline-flex;
        align-items: center;
    }

    .btn-square {
        white-space: nowrap; /* Ngăn chặn nội dung bị xuống dòng */
        padding: 0.5rem 1rem; /* Điều chỉnh khoảng cách nội dung và viền */
        font-size: 1rem; /* Điều chỉnh kích thước font chữ */
    }

        .btn-square:hover {
            background-color: #343a40;
            color: #f8f9fa;
        }

    .cart {
        border-radius: 10px;
        overflow: hidden; /* Đảm bảo không bị cắt bo viền */
        box-sizing: border-box; /* Đảm bảo padding/border không ảnh hưởng đến bo viền */
    }

    .product-item {
        border-radius: 10px;
        overflow: hidden; /* Giúp giữ cho các cạnh được bo tròn */
    }
</style>